<h1>New Question</h1>
<div class="row">
  <div class="col-md-4">
    <p>Guidelines for question</p>
    <ul>
      <li>Create a unique question</li>
      <li>Make sure it is clearly worded</li>
      <li>If you can answer it yourself, don't post it.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @question do |f| %>
    <div class="form-group">
      <%= f.label :title %>
      <%= f.text_field :title, class: 'form-control', placeholder: 'Enter question title' %>
    </div>
    <div class="form-group">
      <%= f.label :body %>
      <%= f.text_area :body, rows: 2, class: 'form-control', placeholder: 'Enter question' %>
    </div>
    <div class="form-group">
      <%= f.submit "Ask Question", class: 'btn btn-success' %>
    </div>
    <% end %>
  </div>
</div>
